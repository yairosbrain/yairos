node_modules
dist
dev-dist
.env
.env.local
.env.*.local
.vercel
.convex
convex/_generated
*.log
.DS_Store

.vercel

# Handoff plaintext — decrypt locally with scripts/vault.mjs, never commit
YAIROS-HANDOFF.md

# Android signing key — back this up OUTSIDE the repo. Never commit.
_keystore/
